<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="gui.Controller"
            prefHeight="400.0" prefWidth="600.0">
    <top>
        <MenuBar>
            <Menu text="Menu">
                <MenuItem text="Sistem Persamaan Linier" onAction="#handleSPL"/>
                <MenuItem text="Determinan" onAction="#handleDeterminan"/>
                <MenuItem text="Matriks balikan" onAction="#handleInverse"/>
                <MenuItem text="Interpolasi Polinom" onAction="#handleInterpolasiPolinom"/>
                <MenuItem text="Interpolasi Bicubic Spline" onAction="#handleInterpolasiBicubic"/>
                <MenuItem text="Regresi linier berganda" onAction="#handleRegresiLinier"/>
                <MenuItem text="Interpolasi Gambar (Bonus)" onAction="#handleInterpolasiGambar"/>
                <MenuItem text="Keluar" onAction="#handleExit"/>
            </Menu>
        </MenuBar>
    </top>
    <center>
        <VBox alignment="CENTER" spacing="10">
            <TextArea fx:id="inputArea" promptText="Masukkan matriks di sini..." />
            <HBox alignment="CENTER" spacing="10">
                <Button text="Hitung" onAction="#handleCalculate"/>
                <Button text="Baca dari File" onAction="#handleReadFile"/>
            </HBox>
            <TextArea fx:id="outputArea" editable="false" promptText="Hasil akan ditampilkan di sini..." />
        </VBox>
    </center>
    <center>
        <VBox alignment="CENTER" spacing="10">
            <TextArea fx:id="inputArea" promptText="Masukkan matriks di sini..." />
            <HBox alignment="CENTER" spacing="10">
                <Button text="Hitung" onAction="#handleCalculate"/>
                <Button text="Baca dari File" onAction="#handleReadFile"/>
            </HBox>
            <TextArea fx:id="outputArea" editable="false" promptText="Hasil akan ditampilkan di sini..." />
        </VBox>
    </center>
    <bottom>
        <Label text="Kelompok 5" />
    </bottom>
</BorderPane>